# Dars 11: Amaliyot. Function va Metod

**Davomiyligi:** 16:14

## 📋 Dars mazmuni

Bu darsda function va metodlarni birgalikda ishlatishni o'rganamiz:
- 8-darsdagi kodlarni function'larga o'tkazish
- Function va metodlarni kombinatsiya qilish
- Real vaziyatlarda qo'llash
- Uyga vazifalarni bajarish

## 🎯 O'rganiladigan mavzular

### Vazifa 1: Eski kodlarni function'larga o'tkazish
- `askNumberOfSeries()` - Serial sonini so'rash
- `askSeriesInfo()` - Serial ma'lumotlarini so'rash
- `showSeriesMessage()` - Xabar ko'rsatish
- `startSeriesCollection()` - Asosiy funksiya

### Vazifa 2: showDb() funksiyasi
- `showDb()` - Database ma'lumotlarini ko'rsatish
- `private` xususiyatini tekshirish
- Shartli ko'rsatish

### Vazifa 3: writeGenres() funksiyasi
- `writeGenres()` - 3 marta janr so'rash
- Massivga element qo'shish
- Loop bilan ishlash

### Qo'shimcha funksiyalar
- `addSeries()` - Serial qo'shish
- `deleteSeries()` - Serial o'chirish
- `updateSeriesRating()` - Baho yangilash
- `setPrivate()` - Private holatini o'zgartirish
- `clearDatabase()` - Database'ni tozalash

## 📁 Fayl strukturası

```
11-amaliyot-function-va-metod/
├── index.html       # Interaktiv HTML interfeys
├── script.js        # Barcha funksiyalar va kodlar
└── README.md       # Bu fayl
```

## 🚀 Qanday ishlatish

1. `index.html` faylini browser'da oching
2. Browser konsolni oching (F12)
3. Har bir vazifa uchun tugmalarni bosing
4. Konsolda natijani ko'ring
5. Kodlarni o'qib, tushunishga harakat qiling

## 📝 Vazifalar

### ✅ Vazifa 1: Eski kodlarni function'larga o'tkazish
- [x] `askNumberOfSeries()` funksiyasi
- [x] `askSeriesInfo()` funksiyasi
- [x] `showSeriesMessage()` funksiyasi
- [x] `startSeriesCollection()` asosiy funksiyasi

### ✅ Vazifa 2: showDb() funksiyasi
- [x] `showDb()` funksiyasi yozildi
- [x] `private` xususiyatini tekshirish
- [x] Shartli ko'rsatish

### ✅ Vazifa 3: writeGenres() funksiyasi
- [x] `writeGenres()` funksiyasi yozildi
- [x] 3 marta foydalanuvchidan janr so'rash
- [x] Massivga element qo'shish

### ✅ Qo'shimcha funksiyalar
- [x] Serial qo'shish/o'chirish
- [x] Baho yangilash
- [x] Private holatini boshqarish
- [x] Database'ni tozalash

## 💡 Maslahatlar

- Har bir funksiyani alohida sinab ko'ring
- Konsolda natijani kuzating
- Kodlarni o'zgartirib, natijani ko'ring
- Function va metodlarni birgalikda ishlatishga harakat qiling

## 📚 Kod misollari

### Vazifa 1: Serial ma'lumotlarini yig'ish
```javascript
startSeriesCollection();
```

### Vazifa 2: Database'ni ko'rsatish
```javascript
showDb(); // private = false bo'lsa ko'rsatadi
```

### Vazifa 3: Janrlar yozish
```javascript
writeGenres(); // 3 marta janr so'raydi
```

## 🎓 O'rganilgan narsalar

- Function declaration va expression
- Function parametrlari va return
- Object metodlari bilan ishlash
- Array metodlari bilan ishlash
- Shartli operatorlar funksiyalarda
- Loop'lar funksiyalarda

