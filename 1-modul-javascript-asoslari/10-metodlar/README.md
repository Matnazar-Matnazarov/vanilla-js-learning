# Dars 10: Metodlar

**Davomiyligi:** 22:10

## 📋 Dars mazmuni

Bu darsda JavaScript'ning eng muhim metodlarini o'rganamiz:
- **String metodlari** - Matnlar bilan ishlash
- **Number metodlari** - Raqamlar bilan ishlash
- **Array metodlari** - Massivlar bilan ishlash
- **Object metodlari** - Obyektlar bilan ishlash

## 🎯 O'rganiladigan mavzular

### String Metodlari
- `length`, `toUpperCase()`, `toLowerCase()`
- `charAt()`, `indexOf()`, `lastIndexOf()`
- `includes()`, `startsWith()`, `endsWith()`
- `slice()`, `substring()`, `substr()`
- `replace()`, `replaceAll()`, `split()`
- `trim()`, `concat()`, `repeat()`
- `padStart()`, `padEnd()`
- Template Literals (ES6)

### Number Metodlari
- `Number()`, `parseInt()`, `parseFloat()`
- `toFixed()`, `toPrecision()`, `toString()`
- `isNaN()`, `isFinite()`
- Math metodlari: `abs()`, `round()`, `ceil()`, `floor()`
- `Math.max()`, `Math.min()`, `Math.pow()`, `Math.sqrt()`
- `Math.random()`

### Array Metodlari
- `length`, `push()`, `pop()`, `unshift()`, `shift()`
- `indexOf()`, `lastIndexOf()`, `includes()`
- `slice()`, `splice()`, `concat()`, `join()`
- `forEach()`, `map()`, `filter()`
- `find()`, `findIndex()`, `some()`, `every()`
- `reduce()`, `sort()`, `reverse()`

### Object Metodlari
- `Object.keys()`, `Object.values()`, `Object.entries()`
- `Object.assign()`, `Object.freeze()`, `Object.seal()`
- `hasOwnProperty()`, `Object.hasOwn()`
- `Object.defineProperty()`, `Object.create()`
- `Object.is()`, `Object.getOwnPropertyNames()`

## 📁 Fayl strukturası

```
10-metodlar/
├── index.html              # Asosiy HTML fayl
├── script.js               # Asosiy script
├── string-metodlari.js     # String metodlari misollari
├── number-metodlari.js     # Number metodlari misollari
├── array-metodlari.js      # Array metodlari misollari
├── object-metodlari.js     # Object metodlari misollari
└── README.md               # Bu fayl
```

## 🚀 Qanday ishlatish

1. `index.html` faylini browser'da oching
2. Browser konsolni oching (F12)
3. Har bir metod turi uchun tugmalarni bosing
4. Konsolda natijani ko'ring
5. Alohida fayllarni o'qib, kodlarni tushunishga harakat qiling

## 📝 Vazifalar

- [ ] String metodlari bilan ishlash
  - [ ] Matnni formatlash
  - [ ] Matndan qismlarni ajratish
  - [ ] Matnlarni birlashtirish
  
- [ ] Number metodlari bilan ishlash
  - [ ] Raqamlarni formatlash
  - [ ] Tasodifiy sonlar generatsiya qilish
  - [ ] Matematik hisob-kitoblar
  
- [ ] Array metodlari bilan ishlash
  - [ ] Massiv elementlarini o'zgartirish
  - [ ] Massiv elementlarini filter qilish
  - [ ] Massiv elementlarini tartiblash
  
- [ ] Object metodlari bilan ishlash
  - [ ] Object xususiyatlarini olish
  - [ ] Object'larni birlashtirish
  - [ ] Object'ni nusxalash

## 💡 Maslahatlar

- Har bir metodni alohida sinab ko'ring
- Konsolda natijani kuzating
- Kodlarni o'zgartirib, natijani ko'ring
- Amaliy loyihalarda qo'llashga harakat qiling

## 📚 Qo'shimcha resurslar

- [MDN String Methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String)
- [MDN Number Methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number)
- [MDN Array Methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)
- [MDN Object Methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object)

